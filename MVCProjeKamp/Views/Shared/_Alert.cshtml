@model EntityLayer.ViewModels.AlertViewModel

@if (Model.Status)
{
    <div class="alert alert-secondary" role="alert">
      @Model.AlertMessage
        <a href="@Model.URL"> @Model.LinkText </a>
    </div>
}
